.venv/
__pycache__/
.pytest_cache/
*.pyc

# Runtime data (SQLite WAL, logs)
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal
data/*.log

# Chart JSON files (generated by scan.py / CI — not committed)
data/charts/

# Generated HTML (produced by build_html.py)
# docs/index.html is committed to git so GitHub Pages "Deploy from branch" can serve it.
# The CI workflow regenerates and re-commits it on every scan.

# Secretos locales
.env
